<?xml version="1.0" encoding="utf-8"?>
<!-- updatePeriodMillis (30 min, the platform minimum) re-draws the widget so a
     list left over from yesterday turns into "open the app" soon after
     midnight even if the app isn't opened. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/today_widget_description"
    android:initialLayout="@layout/today_widget"
    android:previewLayout="@layout/today_widget_preview"
    android:minWidth="250dp"
    android:minHeight="180dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="3"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen" />
